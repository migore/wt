# wt

A small Bash helper for managing Git worktrees.

## Install

Keep the source in this repository and expose it on your PATH with a symlink:

```bash
ln -sfn "$PWD/wt" ~/.local/bin/wt
```

Make sure `~/.local/bin` is on your PATH.
